import { StateButtonVisibilityDirective } from './state-button-visibility.directive';

describe('StateButtonVisibilityDirective', () => {
  it('should create an instance', () => {
    const directive = new StateButtonVisibilityDirective();
    expect(directive).toBeTruthy();
  });
});
